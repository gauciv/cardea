# =============================================================================
# CARDEA DASHBOARD - OAUTH CONFIGURATION (Microsoft & Google)
# =============================================================================
# IMPORTANT: For Vite, all environment variables MUST start with VITE_

# =============================================================================
# MICROSOFT ENTRA EXTERNAL ID CONFIGURATION
# =============================================================================
# Get these values from: https://portal.azure.com -> Microsoft Entra ID -> App registrations

# Your Application (client) ID from Azure Portal
VITE_AZURE_CLIENT_ID=PASTE_YOUR_MICROSOFT_CLIENT_ID_HERE

# Your Azure Tenant ID
# Find this in: Azure Portal -> Microsoft Entra ID -> Overview -> Tenant ID
VITE_AZURE_TENANT_ID=PASTE_YOUR_MICROSOFT_TENANT_ID_HERE

# Tenant Name (for External ID, the subdomain before .ciamlogin.com)
VITE_AZURE_TENANT_NAME=PASTE_YOUR_TENANT_NAME_HERE

# Authority URL - Choose based on your setup:
# Option 1 - For Microsoft Entra External ID with custom domain:
#   https://{your-tenant-name}.ciamlogin.com
# Option 2 - For standard Entra ID (most common):
#   https://login.microsoftonline.com/{TENANT_ID}
# Option 3 - For multi-tenant apps:
#   https://login.microsoftonline.com/common
VITE_AZURE_AUTHORITY=https://YOUR_TENANT_NAME.ciamlogin.com/

# Redirect URI - Must match what you configured in Azure Portal
# Development:
VITE_REDIRECT_URI=http://localhost:5173
# Production (uncomment and update when deploying):
# VITE_REDIRECT_URI=https://your-domain.com

# Post Logout Redirect URI
VITE_POST_LOGOUT_REDIRECT_URI=http://localhost:5173

# =============================================================================
# GOOGLE OAUTH 2.0 CONFIGURATION
# =============================================================================
# Get these values from: https://console.cloud.google.com/apis/credentials

# Your Google OAuth 2.0 Client ID
# Find this in: Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs
VITE_GOOGLE_CLIENT_ID=PASTE_YOUR_GOOGLE_CLIENT_ID_HERE

# Optional: Google Client Secret (only needed for backend token validation)
# Note: Keep this secret and never commit it to version control!
VITE_GOOGLE_CLIENT_SECRET=PASTE_YOUR_GOOGLE_CLIENT_SECRET_HERE

# =============================================================================
# API CONFIGURATION
# =============================================================================

# Oracle Backend API URL
VITE_API_URL=http://localhost:8000
VITE_ORACLE_URL=http://localhost:8000

# Microsoft API Scopes - Format: api://{CLIENT_ID}/access_as_user
# You need to expose this API scope in Azure Portal first
VITE_API_SCOPES=api://PASTE_YOUR_MICROSOFT_CLIENT_ID_HERE/access_as_user

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Enable/disable authentication providers
VITE_ENABLE_AZURE_AUTH=true
VITE_ENABLE_GOOGLE_AUTH=true

# Enable debug logging in development
VITE_DEBUG=true
