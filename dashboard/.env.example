# =============================================================================
# CARDEA DASHBOARD - FRONTEND ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values below
# 3. CRITICAL: All environment variables MUST start with VITE_ prefix for Vite
# 4. WARNING: These values are embedded in the frontend bundle
# =============================================================================

# =============================================================================
# MICROSOFT ENTRA EXTERNAL ID CONFIGURATION
# =============================================================================
# Azure Portal: https://portal.azure.com -> Microsoft Entra ID -> App Registrations

# Your Application (client) ID
VITE_AZURE_CLIENT_ID=your-client-id-here

# Your Azure Tenant ID
VITE_AZURE_TENANT_ID=your-tenant-id-here

# Your Tenant Name/Subdomain (for External ID)
VITE_AZURE_TENANT_NAME=your-tenant

# Authority URL - External ID tenant
# Format: https://{tenant-subdomain}.ciamlogin.com/
VITE_AZURE_AUTHORITY=https://your-tenant.ciamlogin.com/

# Redirect URIs - Must match Azure Portal configuration
# Development
VITE_REDIRECT_URI=http://localhost:5173
# Production (update when deploying): https://your-domain.com

# Post Logout Redirect URI
VITE_POST_LOGOUT_REDIRECT_URI=http://localhost:5173

# =============================================================================
# MICROSOFT API SCOPES
# =============================================================================

# API Scopes - Format: api://{CLIENT_ID}/scope_name
# This must match the scope exposed in Azure Portal -> Expose an API
VITE_API_SCOPES=api://your-client-id-here/access_as_user

# =============================================================================
# GOOGLE OAUTH 2.0 CONFIGURATION
# =============================================================================
# Google Cloud Console: https://console.cloud.google.com/apis/credentials

# Your Google OAuth 2.0 Client ID
# NOTE: Client ID is safe to expose in frontend; secret stays on backend only
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# DO NOT include Google Client Secret in frontend!
# It must remain on the backend for security

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================

# Oracle Backend API Base URL
# Development
VITE_API_URL=http://localhost:8000
VITE_ORACLE_URL=http://localhost:8000

# Production (update when deploying):
# VITE_API_URL=https://api.your-domain.com
# VITE_ORACLE_URL=https://api.your-domain.com

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Enable/disable authentication providers
VITE_ENABLE_AZURE_AUTH=true
VITE_ENABLE_GOOGLE_AUTH=true

# Enable debug logging (shows detailed auth flow in console)
VITE_DEBUG=true

# =============================================================================
# APPLICATION METADATA
# =============================================================================

# Application Environment
VITE_NODE_ENV=development
VITE_APP_NAME=Cardea Dashboard
VITE_APP_VERSION=1.0.0
